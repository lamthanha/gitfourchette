<?xml version='1.0' encoding='utf-8'?>
<component type="desktop">
  <id>org.gitfourchette.gitfourchette</id>
  <name>GitFourchette</name>
  <developer id="io.jor">
    <name>Iliyas Jorio</name>
  </developer>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0</project_license>

  <provides><id>org.gitfourchette.gitfourchette.desktop</id></provides>
  <launchable type="desktop-id">org.gitfourchette.gitfourchette.desktop</launchable>

  <summary>The comfortable Git UI</summary>
  <summary xml:lang="fr">L’interface Git tout confort</summary>

  <description>
    <p>Explore your repos easily. Craft commits intuitively. GitFourchette will become your new home to get work done in your Git repositories.</p>
    <p xml:lang="fr">Explorez vos dépôts facilement. Créez vos commits intuitivement. Vous vous sentirez comme chez vous dans GitFourchette pour accomplir toutes vos tâches dans vos dépôts Git.</p>

    <ul>
      <li>A comfortable way to explore and understand your Git repositories</li>
      <li xml:lang="fr">Une manière conviviale d’explorer et de comprendre vos dépôts Git</li>

      <li>Powerful tools to stage code, create commits, and manage branches</li>
      <li xml:lang="fr">Des outils puissants pour indexer du code, créer des commits, et gérer vos branches</li>

      <li>Snappy and intuitive Qt UI designed to fit in snugly with KDE Plasma</li>
      <li xml:lang="fr">Une interface Qt réactive et intuitive, pensée pour bien s’intégrer dans KDE Plasma</li>
    </ul>
  </description>

  <url type="homepage">https://gitfourchette.org</url>
  <url type="bugtracker">https://github.com/jorio/gitfourchette/issues</url>
  <url type="help">https://gitfourchette.org/guide</url>
  <url type="donation">https://ko-fi.com/jorio</url>
  <url type="vcs-browser">https://github.com/jorio/gitfourchette</url>
  <url type="translate">https://gitfourchette.org/localization</url>

  <content_rating type="oars-1.1" />

  <branding>
    <color type="primary" scheme_preference="light">#bad6f0</color>
    <color type="primary" scheme_preference="dark">#1d4264</color>
  </branding>

  <screenshots>
    <screenshot type="default">
      <image>https://gitfourchette.org/_static/appstream/packshot-shadow-light.png</image>
      <caption>Exploring a repository in GitFourchette (light mode).</caption>
      <caption xml:lang="fr">Exploration d’un dépôt dans GitFourchette (mode clair).</caption>
    </screenshot>
    <screenshot>
      <image>https://gitfourchette.org/_static/appstream/packshot-shadow-dark.png</image>
      <caption>Exploring a repository in GitFourchette (dark mode).</caption>
      <caption xml:lang="fr">Exploration d’un dépôt dans GitFourchette (mode sombre).</caption>
    </screenshot>
  </screenshots>

  <categories>
    <category>Development</category>
    <category>RevisionControl</category>
  </categories>

  <keywords>
    <keyword>git</keyword>
    <keyword>qt</keyword>
  </keywords>

  <releases>
    <release version="1.11.0" date="2026-08-21" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.11.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li><em>Blame specific line:</em> Right click any line in the diff, then select "Blame Line" to find out which commit introduced this specific line.</li>
        <li><em>Bypass pre-commit hooks</em> via dedicated button in CommitDialog (git commit --no-verify). The button is only shown if your repo uses pre-commit hooks.</li>
        <li><em>New platform-agnostic themes</em> (light &amp; dark) provide a nicer default look on non-Qt environments. You can still use any Qt styles you have installed on your system.</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Conflicts: You can now preview the entire "Ours" or "Theirs" revision with one click (eye button in ConflictView)</li>
        <li>Conflicts: Operations "Keep Ours" and "Accept Theirs" now ask for your confirmation and clearly explain what is going to happen</li>
        <li>CloneDialog: "Default clone location" settings are easier to access from the Browse button</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Blame: More accurate revlist when blaming a file in a branch that has diverged from the current branch</li>
        <li>Tab names were elided too aggressively with "tab close button" on and "expand tabs" off</li>
        </ul>
      </description>
    </release>
    <release version="1.10.0" date="2026-08-01" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.10.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li>CommitDialog: Dedicated "signoff" button (git commit --signoff)</li>
        <li>Open repo workdir in external editor (from Repo menu, or by right-clicking a repo tab) (#118)</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Askpass: Better UI when connecting to an unknown host (#128)</li>
        <li>Specific "No changes" message when A/B diffing two commits with the same tree</li>
        <li>Conflict resolution: Improved compatibility with merge tools that write the merged file from a different process after a delay (some popular IDEs)</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix stash deletion failing when trash is disabled (#122)</li>
        <li>Cherrypick/Revert: Show meaningful error message if git exits with code 128, typically "local changes would be overwritten" (#130)</li>
        <li>Flatpak: Fix ssh-agent sandboxed state after changing git executable from settings</li>
        <li>Subpatch extraction: Don't spill over to first line in next hunk if selection ends on hunk header (was only an issue with context lines turned off)</li>
        <li>FileList: Edit HEAD Version in Editor: Get file from HEAD, not index if it has staged changes</li>
        <li>Restore Revision and Save Revision As were rewritten to use git restore/git cat-file, fixing issues with restoring symlinks</li>
        <li>DiffView: Reevaluate search term when switching to a different diff document</li>
        </ul>
        <p>Security fixes:</p>
        <ul>
        <li>Custom Commands that manipulate the selected object via tokens like $FILE or $REF now safely escape the object's name before substitution. In a hostile repo, this prevents malicious filenames or ref names from injecting tokens into a Custom Command that you run on them.</li>
        </ul>
        <p>Maintenance &amp; packaging notes:</p>
        <ul>
        <li>Bump minimum Python version to 3.12 (previously 3.10)</li>
        <li>Make Pygments a mandatory dependency (previously optional)</li>
        <li>Enforce type checking with mypy (#2)</li>
        </ul>
      </description>
    </release>
    <release version="1.9.1" date="2026-07-14" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.9.1</url>
      <description>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Add Ghostty terminal preset (#116)</li>
        <li>More legible text contrast in the sidebar in some desktop environments (Fedora/GNOME)</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix "repo nickname" dialog introduced in 1.9.0 didn't save nickname across sessions</li>
        <li>Fix regression in trash system (first discard after fresh install) (#119)</li>
        <li>Fix "empty trash" didn't clean up trashed symlinks</li>
        </ul>
      </description>
    </release>
    <release version="1.9.0" date="2026-07-01" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.9.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li>Show/hide whitespace symbols (#108)</li>
        <li>Options to ignore whitespace in diffs (#108)</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Tool buttons above DiffView give quick access to: word wrap, whitespace, context lines, SVG preview (#108)</li>
        <li>NewTagDialog offers replacing existing local tags (#102)</li>
        <li>Tabs: Disambiguate tab labels when several repos share the same basename (#113)</li>
        <li>The "repo nickname" feature is now easier to access (right-click on a tab → Rename)</li>
        <li>Blame: Reevaluate search term when switching to another revision</li>
        <li>Allow resizing Commit Info dialog when showing a long commit message (#109)</li>
        <li>Allow Keypad Enter to accept dialogs in non-KDE environments, in addition to the main Return key which was supported since 1.5.0. (Note: KDE already supports this by default.) (#110)</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix benign error message on startup if you ticked a "Don't Show Again" checkbox in a previous session (#112)</li>
        <li>Blame: Fix crash when blaming workdir file during merge in progress</li>
        <li>Handle edge cases in "Added by both"/"Deleted by both" conflicts involving symlinks (#115)</li>
        <li>Fix minor UX papercuts with very long repository tab names</li>
        <li>NewRepo: Fix button label in non-empty directory warning</li>
        <li>NewRepo: Restore "Create" button in nested repository warning</li>
        </ul>
        <p>Maintenance:</p>
        <ul>
        <li>Continued from work started in v1.5.0, more operations were rewritten to be implemented with "standard" Git instead of libgit2 for improved compatibility (New Tag, Delete Tag, Resolve by Keeping Ours/Taking Theirs).</li>
        </ul>
      </description>
    </release>
    <release version="1.8.0" date="2026-05-21" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.8.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li><em>Search for commits touching a file.</em> In the Commit History, press Ctrl+F (or /), then change the search scope from "Info" to "Paths".</li>
        <li><em>Search for refs in the sidebar.</em> In the Sidebar, press Ctrl+F (or /) to filter branches, tags, or stashes.</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix icon for good SSH signature validation when principal name contains spaces (#105)</li>
        <li>Refresh workdir when 'git cherrypick' reports conflicts</li>
        <li>Friendly error message if Blame keyboard shortcut is pressed when no file is selected</li>
        <li>Fix diff title text when returning to a pre-cached text diff</li>
        </ul>
      </description>
    </release>
    <release version="1.7.1" date="2026-04-11" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.7.1</url>
      <description>
        <p>Fix regression in 1.7.0 where RefreshRepo couldn't be canceled effectively.</p>
      </description>
    </release>
    <release version="1.7.0" date="2026-04-09" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.7.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li><em>Compare two commits.</em> Hold the Control key while selecting two commits in the history to diff them.</li>
        <li><em>Improved LFS integration.</em> A special icon appears next to LFS-tracked files. Images and text files stored as LFS can now be diffed. Actions such as "Restore File Revision" are now LFS-aware.</li>
        <li><em>Upstream status indicators in sidebar.</em> Ahead/behind by N commits, upstream branch missing.</li>
        <li><em>Customizable mouse shortcuts in file lists and tab bar.</em> You can now bind the double-click and middle-click to different actions (stage, blame, open, reveal).</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Unstage both addition and deletion when unstaging a rename (#87)</li>
        <li>New "Filename First" path display style (#86)</li>
        <li>Replace spaces with dashes in branch name input fields (#88)</li>
        <li>Consistently crisp icon rendering on high-DPI displays</li>
        <li>Faster large-blob detection (requires pygit2 1.19.2 or newer)</li>
        <li>Queue up to 1 action if busy with another non-interruptible task (replacing the "please wait for current task to complete" dialog)</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix line-by-line staging if filename contains non-ASCII characters (#89)</li>
        <li>Fix sidebar context menu on local branches whose upstream is set to a symbolic ref (#92)</li>
        <li>Resolve gitdir path so symlinked repo roots don't trigger error (#92)</li>
        <li>Fix author signature couldn't be overridden when concluding a cherrypick</li>
        <li>Fix repo wouldn't reload after raising max commit count from "History Truncated" page</li>
        <li>Fix keyboard shortcut conflicts on Cinnamon</li>
        </ul>
      </description>
    </release>
    <release version="1.6.0" date="2026-02-01" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.6.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li><em>Mount commits as folders.</em> This lets you browse the workdir at a specific point in time using your system's file manager. Available by right-clicking a commit in the Commit History. (FUSE 3 required + new optional dependency 'mfusepy')</li>
        <li><em>Auto-fetch remotes periodically (experimental).</em> Enable this <em>Settings → Advanced → Auto-fetch remotes every N minutes.</em></li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>FileList: Improve path readability by muting color of directory string (#77)</li>
        <li>GraphView context menu: Show branch name instead of "Reset HEAD to Here" (#71)</li>
        <li>GraphView context menu: Offer merging for any commit, not just branch tips (#74)</li>
        <li>Image diffs: Ability to toggle between old/new images</li>
        <li>Blame: Emphasize "new" lines with a different background color in the gutter</li>
        <li>Allow pressing the Escape key to quickly close detached diff/blame windows</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix impossible to open worktrees located in a subdirectory of a bare repo (#82)</li>
        <li>When a merge is blocked by untracked files that would be overwritten, show an error message (instead of denying the merge silently)</li>
        </ul>
        <p>Maintenance:</p>
        <ul>
        <li>Continued from the work started in v1.5.0, more operations now use "standard" Git instead of libgit2. This should be transparent to most users; others may benefit from better interoperability with their Git setup. (Reworked operations include: Workdir Status, Commit Diffs, Export Patch, Blame/Revlist, Register/Remove Submodule, New Repository, Add/Remove/Edit Remote, Restore files after stashing)</li>
        <li>Some work on Windows compatibility (experimental)</li>
        </ul>
      </description>
    </release>
    <release version="1.5.0" date="2025-09-08" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.5.0</url>
      <description>
        <p><em>Major change: Better integration with standard Git tooling.</em> GitFourchette now uses git instead of libgit2 to edit repositories and communicate with remotes. This enables seamless integration into workflows that depend on OpenSSH, hooks, etc.</p>
        <p>The Flatpak version comes bundled with a Git distribution so you can use it without any additional setup. For more control, you can switch to your system's Git install via <em>Settings → Git Integration</em>.</p>
        <p>New features:</p>
        <ul>
        <li><em>GPG-sign your commits.</em> Look for a little key icon in the Commit Dialog. (#63)</li>
        <li><em>Verify commit signatures in the commit history.</em> <em>Settings → Commit History → Verify signed commits on the fly</em>. (#59)</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Force push with lease (#61)</li>
        <li>Sidebar: Bold current commit and upstream (#60)</li>
        <li>Sidebar: Allow collapsing/expanding all folders from Local Branches context menu</li>
        <li>Keep syntax highlighting active while a search term is being highlighted</li>
        <li>In non-KDE environments (e.g. GNOME), the Return key can now be used to confirm most dialogs regardless of the focused widget (note: KDE already allowed this) (#68)</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix rendering of Unicode surrogate pairs in character-level diffs (#58)</li>
        <li>Fix parent commit links in Get Commit Info (regression in 1.4.0)</li>
        </ul>
        <p>Breaking changes due to integration with Git tooling:</p>
        <ul>
        <li>Passphrase saving has been delegated to ssh-agent. Some Linux distros like Ubuntu and Fedora provide an ssh-agent out of the box. As a fallback, you can have GitFourchette spin up an ssh-agent for you via <em>Settings → Git Integration → ssh-agent</em>.</li>
        <li>Per-remote custom SSH keys aren't supported anymore. You can set per-host keys in <em>~/.ssh/config</em> to achieve the same effect; or, you can set per-repo custom SSH keys in <em>Repo → Repo Settings → Log in to SSH remotes with custom key file</em>.</li>
        </ul>
        <p>Note: Although many commands now call git, libgit2 (via pygit2) is still used internally to build a model of the repo.</p>
      </description>
    </release>
    <release version="1.4.0" date="2025-07-14" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.4.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li>Blame/File History. Within GitFourchette, right-click any file and select "Blame File"; or, drag any file in your repo from your file manager and drop it onto the main window to view its history.</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>An informative "drop zone" now appears when you drag an item from an external program over the main window. This tells you what will happen upon dropping the item (open repo folder, open repo containing a file, blame file in current repo, apply patch file, clone URL).</li>
        <li>CheckoutCommitDialog: Also offer Merge, Reset HEAD</li>
        <li>New branch/tag name validation: Friendly warning if attempting to create a folder with the same name as an existing ref</li>
        <li>CodeView: Toggling Word Wrap now preserves your scroll position in the document</li>
        <li>FileList: Reevaluate search term when jumping to another commit</li>
        <li>DiffGutter: Enable high-DPI custom cursor on Wayland</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix "Detect Renames" may unexpectedly switch to the workdir in rare cases</li>
        <li>Flatpak distribution: Updated to Pygments 2.19.2 to fix problems with Lua syntax highlighting</li>
        </ul>
      </description>
    </release>
    <release version="1.3.0" date="2025-05-03" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.3.0</url>
      <description>
        <p><em>Upgrade note:</em> The terminal command template now requires the <code>$COMMAND</code> argument placeholder. Please review your terminal command in <em>Settings → External Tools → Terminal</em>.</p>
        <p>New features:</p>
        <ul>
        <li>Custom terminal commands (Settings → Custom Commands) (#37).</li>
        <li>Sidebar: Hide All But This (#49). You can now show a single branch and hide all others.</li>
        <li>Add file to .gitignore or .git/info/exclude from untracked file context menu (#42)</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>GraphView: Conjoined refboxes when a local branch is in sync with its upstream (#12)</li>
        <li>Add target branch shorthand to default merge commit message (#39)</li>
        <li>NewBranchDialog: Pre-tick upstream checkbox if branching off remote branch</li>
        <li>Report mandatory placeholder token errors when launching external tool commands</li>
        <li>Speed up remote listing and upstream lookup</li>
        <li>Fix Sidebar/DiffArea would sometimes jump around by a few pixels while temporary banners were shown</li>
        <li>Override Yaru icon theme's "scary" red warning icon (#27)</li>
        <li>More helpful message if Python bindings for QtSvg are missing</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Sidebar: Strip initial slash in nested RefFolder display names (#45)</li>
        <li>Fix Ctrl+G keyboard shortcut on GNOME</li>
        <li>Work around window snapping issue on GNOME (#50)</li>
        </ul>
        <p>Two other bug fixes originally introduced in v1.2.0 had been dormant until now due to depending on a pygit2 version bump – they are now in full effect:</p>
        <ul>
        <li>Fix push progress wasn't reported properly during transfer (requires pygit2 1.18.0) (#22)</li>
        <li>Fix push couldn't be canceled once the transfer starts (requires pygit2 1.18.0) (#22)</li>
        </ul>
      </description>
    </release>
    <release version="1.2.1" date="2025-03-05" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.2.1</url>
      <description>
        <p>User-suggested quality of life improvements:</p>
        <ul>
        <li>GraphView: Copy commit message from context menu (#33)</li>
        <li>Rephrase some messages (diff too large, delete untracked file) (#27, #28)</li>
        <li>Add Ptyxis terminal preset (#18)</li>
        </ul>
        <p>Other quality of life improvements:</p>
        <ul>
        <li>Friendlier messages related to 'autocrlf' and 'safecrlf' options (#30)</li>
        <li>Flatpak distribution: Detach terminal process from application</li>
        <li>Improve contrast between enabled/disabled SVG icons throughout the UI</li>
        <li>Pasting a multiline commit message into CommitDialog's summary input box will correctly split the message across the summary and description boxes</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix error when an external program converts line endings in the workdir with 'autocrlf' (#30)</li>
        <li>Flatpak distribution: Fix terminal spawned in incorrect working directory (#18)</li>
        <li>Flatpak distribution: Fix Flatpak tool existence check</li>
        <li>Gracefully handle destruction of parent widget of external tool processes</li>
        </ul>
      </description>
    </release>
    <release version="1.2.0" date="2025-02-19" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.2.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li>Open terminal in workdir (#18)</li>
        </ul>
        <p>User-suggested quality of life improvements:</p>
        <ul>
        <li>Reword "Uncommitted changes" to "Working directory" to clear up any confusion when there are no changes (#13)</li>
        <li>Always display the number of uncommitted changes in the sidebar and in the graph; update this whenever the app returns to the foreground (#13)</li>
        <li>Reword "Discard Changes" context menu entry for untracked files to "Delete File" (#17)</li>
        <li>Global ref sorting setting (#20)</li>
        <li>Allow creating a merge commit without fast-forwarding (#21)</li>
        <li>Dark mode readability tweaks (#24)</li>
        <li>Command line: Open arbitrary nested paths in a repo (#25)</li>
        </ul>
        <p>Other quality of life improvements:</p>
        <ul>
        <li>Improve consistency of ellipses in menus to signify that an action can be canceled</li>
        <li>Warn user if an external Flatpak isn't installed when attempting to run it (merge tool, terminals, etc.)</li>
        <li>Remind user about any unstaged files when about to create an empty commit</li>
        <li>Allow middle-clicking to quickly stage/unstage selected lines in DiffView (enable in Settings → Advanced → Middle-click)</li>
        <li>Allow clearing draft commit messages by right-clicking the top row in GraphView</li>
        <li>After pushing a branch that doesn't track an upstream, PushDialog will suggest the remote branch you used previously (instead of defaulting to the first one in the repo)</li>
        <li>Pulling will automatically fast-forward if possible without an extra confirmation step, unless your git config contains "pull.ff=false" (behavior aligned to vanilla git). If fast-forwarding isn't possible, you will still be prompted to merge.</li>
        <li>Push/fetch status text uses the 'tnum' OpenType feature to avoid transfer rate numbers jumping around if your system font has digits with uneven widths</li>
        <li>AppImage distribution now compatible with fuse3</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix crash when closing several repos in quick succession, e.g. by holding down Ctrl+W</li>
        <li>Fix clicking the help button used to close the fast-forward dialog</li>
        <li>Fix DiffView out of sync with rest of RepoWidget after hiding a the currently-selected branch tip</li>
        <li>Fix italics in sidebar didn't update after changing the current branch's upstream (a remote-tracking branch in italics means it's the upstream for the current branch)</li>
        <li>Fix visual artifacts around character-level diffs in DiffView</li>
        <li>Fix push progress wasn't reported properly during transfer (requires pygit2 1.18.0) (#22)</li>
        <li>Fix push couldn't be canceled once the transfer starts (requires pygit2 1.18.0) (#22)</li>
        </ul>
        <p>Breaking changes:</p>
        <ul>
        <li>The keyboard shortcut for "Go to Working Directory" is now Ctrl+G (formerly Ctrl+U, for "Uncommitted Changes"). On most keyboard layouts, Ctrl+G pairs nicely with Ctrl+H for "Go to HEAD".</li>
        </ul>
      </description>
    </release>
    <release version="1.1.1" date="2025-01-19" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.1.1</url>
      <description>
        <p>New features:</p>
        <ul>
        <li>Option to remember passphrases in encrypted keyfiles (#15)</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Omit remote name from refboxes when there's just 1 remote (#11)</li>
        <li>Display blob hashes in FileList tooltips</li>
        <li>GraphView tries to use the 'tnum' OpenType feature to align ISO-8601 dates if your system font has digits with uneven widths</li>
        <li>In commit/filename SearchBars, don't re-trigger a search if appending to a word that is known to have no occurrences</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix custom key file feature in CloneDialog and AddRemote</li>
        <li>Fix remote branch context menu in a repo with an unborn head</li>
        <li>GraphSplicer: Fix discrete branch may vanish from graph if moved past another branch by topological sorting</li>
        </ul>
      </description>
    </release>
    <release version="1.1.0" date="2025-01-05" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.1.0</url>
      <description>
        <p>New features:</p>
        <ul>
        <li>Syntax highlighting with Pygments (optional dependency)</li>
        </ul>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Customizable ref indicator width (Settings → Commit History) (#10)</li>
        <li>Condensed fonts can be disabled (Settings → Advanced) (#10)</li>
        <li>Improved settings dialog UI</li>
        </ul>
      </description>
    </release>
    <release version="1.0.2" date="2024-12-16" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.0.2</url>
      <description>
        <p>Usability improvements:</p>
        <ul>
        <li>Support launching a Flatpak as an external diff/merge tool (#4)</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix web URLs in remote-tracking branch context menus (e.g. visit a branch on github.com)</li>
        </ul>
      </description>
    </release>
    <release version="1.0.1" date="2024-12-03" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.0.1</url>
      <description>
        <p>Quality of life improvements:</p>
        <ul>
        <li>Friendlier ConflictView UI</li>
        <li>Improve 3-way merging with external merge tools when there's no ancestor in the conflict</li>
        <li>Distinguish submodules and subtrees in SpecialDiff verbiage</li>
        </ul>
        <p>Bug fixes:</p>
        <ul>
        <li>Fix discarding untracked non-submodule subtrees (#1)</li>
        </ul>
      </description>
    </release>
    <release version="1.0.0" date="2024-11-16" type="stable">
      <url>https://github.com/jorio/gitfourchette/releases/tag/v1.0.0</url>
      <description>
        <ul>
        <li>First public release</li>
        </ul>
      </description>
    </release>
  </releases>
</component>
